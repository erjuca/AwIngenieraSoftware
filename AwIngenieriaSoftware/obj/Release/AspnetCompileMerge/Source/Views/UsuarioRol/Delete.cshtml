@model AwIngenieriaSoftware.Models.UsuarioRol

@{
    ViewBag.Title = "Eliminar";
}
<div class="panel panel-info">
    <div class="panel panel-heading">
        <div class="btn-group float-right">
            <span>
                <a href="@Url.Action("Index")" class="btn btn-primary btn-sm ">
                    <span class="fas fa-arrow-left"></span>
                    Regresar a la lista
                </a>
            </span>
        </div>

        <h4><i class="fas fa-pencil-alt"></i> ¿Está seguro que desea eliminar esta asignación?</h4>

    </div>
    <div class="panel panel-body">
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Usuario.UserName)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Usuario.UserName)
            </dd>
            <dt>
                @Html.DisplayNameFor(model => model.Rol.Name)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Rol.Name)
            </dd>
        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()
            <p>
                <div class="form-group">
                    <button type="submit" class="btn btn-danger btn-sm">
                        <i class="fas fa-trash"></i> Eliminar
                    </button>
                </div>
            </p>
        }
    </div>
</div>
